<!--pages/all/index.wxml-->
<van-grid clickable column-num="2">
  <van-grid-item
    wx:for="{{pages}}"
    icon="home-o"
    link-type="navigateTo"
    url="/{{item}}"
    text="{{item}}"
  />
</van-grid>
